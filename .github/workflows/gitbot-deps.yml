# =============================================================================
# AUTO DEPENDENCY UPDATE - T-800 (SIMPLIFIED)
# =============================================================================
# Features:
# - Daily auto-update dependencies (08:00 WIB / 01:00 UTC)
# - Self-healing: auto-updates GitHub Actions in workflows
# - Security audit before commit
# - Test validation
# - Signed-off commits (no GPG signing)
# - Powered by T-800 automation

name: "T-800 - Auto Update Dependencies"
# Disable
#'on':
#  schedule:
#    - cron: "0 1 * * *"  # 08:00 WIB daily
#  workflow_dispatch:
#    inputs:
#      strategy:
#        description: "How to publish updates"
#        type: choice
#        options:
#          - direct
#          - pr
#        default: direct
#      force_update:
#        description: "Continue even if tests fail"
#        type: boolean
#        default: false
#      update_workflows:
#        description: "Also update GitHub Actions versions"
#        type: boolean
#        default: true
#      allow_major_updates:
#        description: "Allow major version updates for GitHub Actions"
#        type: boolean
#        default: false

permissions:
  contents: write
  actions: read
  pull-requests: write

env:
  CARGO_TERM_COLOR: always
  BOT_TOKEN: ${{ secrets.T800_TOKEN || github.token }}
  HAS_PAT: ${{ secrets.T800_TOKEN != '' }}
  UPDATE_STRATEGY: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.strategy || 'direct' }}
  FORCE_UPDATE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.force_update || 'false' }}
  UPDATE_WORKFLOWS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.update_workflows || 'true' }}
  ALLOW_MAJOR_UPDATES: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.allow_major_updates || 'false' }}

jobs:
  auto-update:
    name: "🤖 T-800 - Dependency & Workflow Update"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.3.1
        with:
          token: ${{ env.BOT_TOKEN }}
          fetch-depth: 0

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.81.0
          components: rustfmt, clippy

      - name: Install stable toolchain
        run: rustup toolchain install stable

      - name: Setup cache
        uses: Swatinem/rust-cache@v2.8.2
        with:
          cache-on-failure: true

      - name: Set cargo build jobs
        shell: bash
        run: |
          set -euo pipefail
          JOBS="$(getconf _NPROCESSORS_ONLN 2>/dev/null || true)"
          if [[ -z "${JOBS}" ]]; then
            JOBS="$(nproc 2>/dev/null || true)"
          fi
          if [[ -z "${JOBS}" ]]; then
            JOBS="$(sysctl -n hw.ncpu 2>/dev/null || true)"
          fi
          if [[ -z "${JOBS}" ]]; then
            JOBS=2
          fi
          echo "CARGO_BUILD_JOBS=${JOBS}" >> "$GITHUB_ENV"

      # =============================================================================
      # SELF-HEALING: AUTO-UPDATE GITHUB ACTIONS VERSIONS
      # =============================================================================
      - name: 🤖 Self-Update GitHub Actions
        if: env.UPDATE_WORKFLOWS == 'true' && env.HAS_PAT == 'true'
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "🔍 Scanning for outdated GitHub Actions..."
          if [ "${{ env.ALLOW_MAJOR_UPDATES }}" = "true" ]; then
            echo "⚡ Mode: AGGRESSIVE (includes major version updates)"
          else
            echo "🛡️  Mode: SAFE (minor/patch updates only)"
          fi
          echo "════════════════════════════════════════════════════════"

          UPDATED=false
          UPDATES_LOG=""
          SKIPPED_MAJORS=""
          ACTIONS_UPDATE_LOG="${RUNNER_TEMP}/actions-updates.log"
          SKIPPED_LOG="${RUNNER_TEMP}/skipped-majors.log"
          : > "${ACTIONS_UPDATE_LOG}"
          : > "${SKIPPED_LOG}"

          AUTH_HEADER="Authorization: Bearer ${BOT_TOKEN}"
          ALLOW_MAJOR="${{ env.ALLOW_MAJOR_UPDATES }}"

          for workflow in .github/workflows/*.yml .github/workflows/*.yaml; do
            [ -f "$workflow" ] || continue

            echo ""
            echo "📄 Checking: $workflow"

            while read -r action_with_ver; do
              ACTION="${action_with_ver%@*}"
              CURRENT_VER="${action_with_ver#*@}"

              [[ "$CURRENT_VER" =~ ^v[0-9] ]] || continue

              echo "  🔎 Checking: $ACTION @ $CURRENT_VER"

              if [[ "$ACTION" == */* ]]; then
                # Get latest release
                LATEST_TAG=$(curl -s -H "$AUTH_HEADER" "https://api.github.com/repos/$ACTION/releases/latest" | grep -oP '"tag_name":\s*"\K[^"]+' || echo "")

                if [ -z "$LATEST_TAG" ]; then
                  LATEST_TAG=$(curl -s -H "$AUTH_HEADER" "https://api.github.com/repos/$ACTION/tags" | grep -oP '"name":\s*"v\K[0-9]+(\.[0-9]+)*' | head -n1)
                  [ -n "$LATEST_TAG" ] && LATEST_TAG="v$LATEST_TAG"
                fi

                if [ -n "$LATEST_TAG" ] && [ "$LATEST_TAG" != "$CURRENT_VER" ]; then
                  CURRENT_MAJOR="${CURRENT_VER#v}"
                  CURRENT_MAJOR="${CURRENT_MAJOR%%.*}"
                  LATEST_MAJOR="${LATEST_TAG#v}"
                  LATEST_MAJOR="${LATEST_MAJOR%%.*}"

                  # Check if major version changed
                  if [ "$CURRENT_MAJOR" != "$LATEST_MAJOR" ]; then
                    if [ "$ALLOW_MAJOR" = "true" ]; then
                      echo "    ⚡ Major update available: $CURRENT_VER → $LATEST_TAG (applying)"
                      sed -i "s|$ACTION@$CURRENT_VER|$ACTION@$LATEST_TAG|g" "$workflow"
                      UPDATED=true
                      UPDATES_LOG="${UPDATES_LOG}\n  - $ACTION: $CURRENT_VER → $LATEST_TAG ⚡ (major)"
                      echo "$ACTION: $CURRENT_VER -> $LATEST_TAG (major)" >> "${ACTIONS_UPDATE_LOG}"
                    else
                      echo "    ⚠️  Major version available: $CURRENT_VER → $LATEST_TAG (skipped, enable allow_major_updates)"
                      SKIPPED_MAJORS="${SKIPPED_MAJORS}\n  - $ACTION: $CURRENT_VER → $LATEST_TAG"
                      echo "$ACTION: $CURRENT_VER -> $LATEST_TAG" >> "${SKIPPED_LOG}"

                      # Still try to get latest patch in current major version
                      ALL_TAGS=$(curl -s -H "$AUTH_HEADER" "https://api.github.com/repos/$ACTION/tags" | grep -oP '"name":\s*"v\K[0-9]+(\.[0-9]+)*' || echo "")
                      LATEST_IN_MAJOR=""
                      while IFS= read -r tag; do
                        [ -z "$tag" ] && continue
                        TAG_MAJOR="${tag%%.*}"
                        if [ "$TAG_MAJOR" = "$CURRENT_MAJOR" ]; then
                          LATEST_IN_MAJOR="v$tag"
                          break
                        fi
                      done <<< "$ALL_TAGS"

                      if [ -n "$LATEST_IN_MAJOR" ] && [ "$LATEST_IN_MAJOR" != "$CURRENT_VER" ]; then
                        echo "    ✨ Patch update available: $CURRENT_VER → $LATEST_IN_MAJOR"
                        sed -i "s|$ACTION@$CURRENT_VER|$ACTION@$LATEST_IN_MAJOR|g" "$workflow"
                        UPDATED=true
                        UPDATES_LOG="${UPDATES_LOG}\n  - $ACTION: $CURRENT_VER → $LATEST_IN_MAJOR"
                        echo "$ACTION: $CURRENT_VER -> $LATEST_IN_MAJOR" >> "${ACTIONS_UPDATE_LOG}"
                      fi
                    fi
                  else
                    # Same major version, safe to update
                    echo "    ✨ Update available: $CURRENT_VER → $LATEST_TAG"
                    sed -i "s|$ACTION@$CURRENT_VER|$ACTION@$LATEST_TAG|g" "$workflow"
                    UPDATED=true
                    UPDATES_LOG="${UPDATES_LOG}\n  - $ACTION: $CURRENT_VER → $LATEST_TAG"
                    echo "$ACTION: $CURRENT_VER -> $LATEST_TAG" >> "${ACTIONS_UPDATE_LOG}"
                  fi
                else
                  echo "    ✅ Already up-to-date"
                fi
              fi
            done < <(grep -oP 'uses:\s+\K[^@\s]+@v[0-9]+(\.[0-9]+)*' "$workflow" | sort -u)
          done

          echo ""
          echo "════════════════════════════════════════════════════════"

          echo "actions_update_log=${ACTIONS_UPDATE_LOG}" >> "$GITHUB_ENV"
          echo "skipped_majors_log=${SKIPPED_LOG}" >> "$GITHUB_ENV"

          if [ "$UPDATED" = true ]; then
            echo "✨ GitHub Actions updated successfully!"
            echo -e "$UPDATES_LOG"
            echo "workflows_updated=true" >> "$GITHUB_ENV"
          else
            echo "✅ All GitHub Actions are up-to-date (in current major versions)"
            echo "workflows_updated=false" >> "$GITHUB_ENV"
          fi

          if [ -s "${SKIPPED_LOG}" ]; then
            echo ""
            echo "⚠️  Skipped major version updates:"
            echo -e "$SKIPPED_MAJORS"
            echo ""
            echo "💡 To update major versions, run workflow_dispatch with allow_major_updates=true"
            echo "workflows_has_major_updates=true" >> "$GITHUB_ENV"
          else
            echo "workflows_has_major_updates=false" >> "$GITHUB_ENV"
          fi

          echo "════════════════════════════════════════════════════════"

      # =============================================================================
      # RUST DEPENDENCIES UPDATE
      # =============================================================================
      - name: Check current versions
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "📦 Current Rust dependency versions:"
          echo "════════════════════════════════════════════════════════"
          cargo tree --depth 1 || true
          echo "════════════════════════════════════════════════════════"

      - name: Baseline security audit (pre-update)
        id: pre_audit
        shell: bash
        run: |
          set -euo pipefail
          echo "🔒 Running baseline security audit (pre-update)..."

          cargo +stable install cargo-audit --locked

          if cargo +stable audit; then
            echo "vulnerable=false" >> "$GITHUB_OUTPUT"
          else
            echo "vulnerable=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Update dependencies
        id: update_deps
        run: |
          echo "🔄 Updating Cargo.lock..."
          CARGO_UPDATE_LOG="${RUNNER_TEMP}/cargo-update.log"
          echo "cargo_update_log=${CARGO_UPDATE_LOG}" >> "$GITHUB_ENV"
          cargo update 2>&1 | tee "${CARGO_UPDATE_LOG}"

          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "📦 Updated dependency tree:"
          echo "════════════════════════════════════════════════════════"
          cargo tree --depth 1 || true
          echo "════════════════════════════════════════════════════════"

      - name: Security audit
        id: post_audit
        run: |
          echo "🔒 Running security audit..."
          cargo +stable audit

      - name: Dependency policy (cargo-deny)
        id: deny
        run: |
          echo "🛡️  Checking dependency policies..."
          cargo +stable install cargo-deny --locked
          cargo +stable deny check all

      - name: Format check
        id: fmt
        run: |
          echo "🎨 Checking code formatting..."
          cargo fmt --all -- --check

      - name: Build project
        id: build
        run: |
          echo "🔨 Building with updated dependencies..."
          cargo build --profile dev --locked --jobs "${CARGO_BUILD_JOBS}"
          cargo build --profile release --locked --jobs "${CARGO_BUILD_JOBS}"
          RUSTFLAGS="-C target-cpu=x86-64" cargo build --profile pro-linux-v1 --locked --target x86_64-unknown-linux-gnu --jobs "${CARGO_BUILD_JOBS}"

          echo ""
          echo "✅ Build successful with updated deps"

      - name: Run tests
        id: tests
        run: |
          echo "🧪 Running test suite..."
          cargo test --all --locked

          echo ""
          echo "✅ All tests passed"
        continue-on-error: ${{ env.FORCE_UPDATE == 'true' }}

      - name: Quick clippy check
        id: clippy
        run: |
          echo "🔍 Running clippy sanity check..."
          cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Check for changes
        id: check_changes
        run: |
          if [[ -z "$(git status --porcelain)" ]]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "✅ No updates needed"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "📝 Updates detected"
            echo ""
            echo "Changed files:"
            git status --porcelain
            echo ""
            echo "Diff stat:"
            git diff --stat
          fi

      - name: Generate commit message
        id: commit_msg
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          WORKFLOW_UPDATED="${{ env.workflows_updated }}"
          ACTIONS_UPDATE_LOG="${{ env.actions_update_log }}"

          PRE_AUDIT_VULNERABLE="${{ steps.pre_audit.outputs.vulnerable }}"
          SECURITY_UPDATE="false"
          if [ "${PRE_AUDIT_VULNERABLE}" = "true" ]; then
            SECURITY_UPDATE="true"
          fi

          CARGO_UPDATE_LOG="${{ env.cargo_update_log }}"
          UPDATES_COUNT="0"
          if [ -n "${CARGO_UPDATE_LOG}" ] && [ -f "${CARGO_UPDATE_LOG}" ]; then
            UPDATES_COUNT="$(grep -cE '^[[:space:]]*(Updating|Downgrading)[[:space:]]+' "${CARGO_UPDATE_LOG}" || true)"
            if [ -z "${UPDATES_COUNT}" ]; then
              UPDATES_COUNT="0"
            fi
          fi

          BREAKING_UPDATE="false"
          if [ -n "${CARGO_UPDATE_LOG}" ] && [ -f "${CARGO_UPDATE_LOG}" ]; then
            while IFS= read -r line; do
              if [[ "${line}" =~ ^[[:space:]]*(Updating|Downgrading)[[:space:]]+[^[:space:]]+[[:space:]]+v([0-9]+)\.([0-9]+)\.([0-9]+)[[:space:]]+\-\>[[:space:]]+v([0-9]+)\.([0-9]+)\.([0-9]+) ]]; then
                OLD_MAJOR="${BASH_REMATCH[2]}"
                OLD_MINOR="${BASH_REMATCH[3]}"
                NEW_MAJOR="${BASH_REMATCH[5]}"
                NEW_MINOR="${BASH_REMATCH[6]}"

                if [ "${OLD_MAJOR}" != "${NEW_MAJOR}" ]; then
                  BREAKING_UPDATE="true"
                  break
                fi

                if [ "${OLD_MAJOR}" = "0" ] && [ "${OLD_MINOR}" != "${NEW_MINOR}" ]; then
                  BREAKING_UPDATE="true"
                  break
                fi
              fi
            done < "${CARGO_UPDATE_LOG}"
          fi

          LABELS=$'dependencies\nautomated\n🤖 bot'
          if [ "${SECURITY_UPDATE}" = "true" ]; then
            LABELS+=$'\nsecurity'
          fi
          if [ "${BREAKING_UPDATE}" = "true" ]; then
            LABELS+=$'\nbreaking'
          fi
          if [ "${SECURITY_UPDATE}" != "true" ] && [ "${BREAKING_UPDATE}" != "true" ]; then
            LABELS+=$'\nenhancement'
          fi

          {
            echo "yamllabels<<EOF"
            printf "%s\n" "${LABELS}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          if [ "${SECURITY_UPDATE}" = "true" ] && [ "${WORKFLOW_UPDATED}" = "true" ]; then
            TITLE="fix(security): T-800 patches vulnerable deps + refreshes workflows"
          elif [ "${SECURITY_UPDATE}" = "true" ]; then
            TITLE="fix(security): T-800 patches vulnerable dependencies"
          elif [ "${BREAKING_UPDATE}" = "true" ] && [ "${WORKFLOW_UPDATED}" = "true" ]; then
            TITLE="chore(deps)!: T-800 recalibrates dependencies & workflows"
          elif [ "${BREAKING_UPDATE}" = "true" ]; then
            TITLE="chore(deps)!: T-800 dependency recalibration"
          elif [ "${WORKFLOW_UPDATED}" = "true" ]; then
            TITLE="chore: T-800 recalibrates dependencies & workflows"
          else
            TITLE="chore(deps): T-800 dependency recalibration"
          fi

          TESTS_OUTCOME="${{ steps.tests.outcome }}"
          TESTS_STATUS="✅ passed"
          if [ "${TESTS_OUTCOME}" != "success" ]; then
            if [ "${FORCE_UPDATE}" = "true" ]; then
              TESTS_STATUS="⚠️ failed (forced)"
            else
              TESTS_STATUS="❌ failed"
            fi
          fi

          BASELINE_SECURITY_STATUS="✅ clean"
          if [ "${SECURITY_UPDATE}" = "true" ]; then
            BASELINE_SECURITY_STATUS="⚠️ advisories found"
          fi

          BODY="🤖 Automated update by T-800"
          BODY+=$'\n\n'"## Summary"
          BODY+=$'\n'"- 📦 Rust dependencies updated via \`cargo update\` (${UPDATES_COUNT} changes)"
          BODY+=$'\n'"- 🔐 Security baseline (pre-update): ${BASELINE_SECURITY_STATUS}"
          BODY+=$'\n'"- 🔒 Security audit (post-update): ✅ passed"
          BODY+=$'\n'"- 🛡️ Dependency policy (cargo-deny): ✅ passed"
          BODY+=$'\n'"- 🎨 Formatting (cargo fmt): ✅ passed"
          BODY+=$'\n'"- 🔨 Build (dev + release): ✅ passed"
          BODY+=$'\n'"- 🧪 Tests: ${TESTS_STATUS}"
          BODY+=$'\n'"- 🔍 Clippy: ✅ passed"

          if [ "${WORKFLOW_UPDATED}" = "true" ] && [ -n "${ACTIONS_UPDATE_LOG}" ] && [ -s "${ACTIONS_UPDATE_LOG}" ]; then
            BODY="${BODY}"$'\n\n'"## GitHub Actions updated"
            while IFS= read -r line; do
              [ -n "${line}" ] || continue
              BODY="${BODY}"$'\n'"- ${line}"
            done < "${ACTIONS_UPDATE_LOG}"
          fi

          # Add skipped major updates notice
          SKIPPED_MAJORS_LOG="${{ env.skipped_majors_log }}"
          HAS_MAJOR_UPDATES="${{ env.workflows_has_major_updates }}"
          if [ "${HAS_MAJOR_UPDATES}" = "true" ] && [ -n "${SKIPPED_MAJORS_LOG}" ] && [ -s "${SKIPPED_MAJORS_LOG}" ]; then
            BODY="${BODY}"$'\n\n'"## ⚠️ Major version updates available (skipped)"
            BODY="${BODY}"$'\n'"The following actions have major version updates available but were skipped for safety:"
            BODY="${BODY}"$'\n'
            while IFS= read -r line; do
              [ -n "${line}" ] || continue
              BODY="${BODY}"$'\n'"- ${line}"
            done < "${SKIPPED_MAJORS_LOG}"
            BODY="${BODY}"$'\n\n'"💡 To apply major updates, run this workflow manually with \`allow_major_updates=true\`"
          fi

          if [ -n "${CARGO_UPDATE_LOG}" ] && [ -f "${CARGO_UPDATE_LOG}" ]; then
            UPDATES_SECTION=false
            while IFS= read -r line; do
              [ -n "${line}" ] || continue

              if [ "${UPDATES_SECTION}" != "true" ]; then
                BODY="${BODY}"$'\n\n'"## Dependency updates (top 25)"
                UPDATES_SECTION=true
              fi

              BODY="${BODY}"$'\n'"- ${line}"
            done < <((grep -E '^[[:space:]]*(Updating|Downgrading)[[:space:]]+' "${CARGO_UPDATE_LOG}" || true) | head -n 25 | sed -E 's/^[[:space:]]+//')
          fi

          if [ "${SECURITY_UPDATE}" = "true" ]; then
            BODY="${BODY}"$'\n\n'"## Security"$'\n'"- Baseline audit found advisories before the update"
          fi

          if [ "${BREAKING_UPDATE}" = "true" ]; then
            BODY="${BODY}"$'\n\n'"## Breaking change risk"$'\n'"- Detected a major (or 0.x minor) version jump in Cargo.lock"
          fi

          COMMIT_BODY="${BODY}"

          {
            echo "title=$TITLE"
            echo "body<<EOF"
            echo "$BODY"
            echo "EOF"
            echo "commit_body<<EOF"
            echo "$COMMIT_BODY"
            echo "EOF"
            echo "commit_message<<EOF"
            echo "$TITLE"
            echo ""
            echo "$COMMIT_BODY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true' && env.UPDATE_STRATEGY == 'direct'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local commit.gpgsign false
          git config --local tag.gpgsign false

          if [ "${HAS_PAT}" != "true" ]; then
            if git diff --name-only | grep -q '^\.github/workflows/'; then
              echo "⚠️  Workflow file changes detected but no PAT is configured; dropping workflow changes to avoid push rejection"
              git restore .github/workflows
            fi
          fi

          git add -A
          git commit --no-gpg-sign -s \
            -m "${{ steps.commit_msg.outputs.title }}" \
            -m "${{ steps.commit_msg.outputs.commit_body }}"

          git push origin main

          echo ""
          echo "✅ Updates pushed to main branch"
          echo "📍 Commit: $(git rev-parse HEAD)"

      - name: Create pull request
        id: cpr
        if: steps.check_changes.outputs.changed == 'true' && env.UPDATE_STRATEGY == 'pr'
        uses: peter-evans/create-pull-request@v6.1.0
        with:
          token: ${{ env.BOT_TOKEN }}
          commit-message: "${{ steps.commit_msg.outputs.commit_message }}"
          title: "${{ steps.commit_msg.outputs.title }}"
          body: "${{ steps.commit_msg.outputs.body }}"
          branch: bot/t-800-auto-update
          delete-branch: true
          signoff: true
          labels: ${{ steps.commit_msg.outputs.yamllabels }}
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>

      - name: Summary
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "🤖 T-800 - UPDATE SUMMARY"
          echo "════════════════════════════════════════════════════════"
          echo ""
          echo "Strategy: ${{ env.UPDATE_STRATEGY }}"
          echo "Workflow Updates: ${{ env.UPDATE_WORKFLOWS }}"
          echo "Allow Major Updates: ${{ env.ALLOW_MAJOR_UPDATES }}"
          echo ""

          if [ "${{ steps.check_changes.outputs.changed }}" == "true" ]; then
            echo "✨ Updates Applied:"

            if [ "${{ env.workflows_updated }}" == "true" ]; then
              echo "  - 🔄 GitHub Actions updated"
            fi
            echo "  - 📦 Rust dependencies updated"
            echo "  - 🔒 Security audit passed"
            if [ "${{ steps.tests.outcome }}" == "success" ]; then
              echo "  - 🧪 Tests passed"
            else
              if [ "${{ env.FORCE_UPDATE }}" == "true" ]; then
                echo "  - 🧪 Tests failed (forced)"
              else
                echo "  - 🧪 Tests failed"
              fi
            fi

            if [ "${{ env.workflows_has_major_updates }}" == "true" ]; then
              echo ""
              echo "⚠️  Major version updates available (not applied)"
              echo "   Run with allow_major_updates=true to apply them"
            fi

            echo ""

            if [ "${{ env.UPDATE_STRATEGY }}" == "direct" ]; then
              echo "✅ Changes pushed to main branch"
              echo "📍 Commit: $(git rev-parse HEAD)"
            else
              echo "✅ Pull request created"
              echo "🔗 PR: ${{ steps.cpr.outputs.pull-request-url }}"
            fi
          else
            echo "✅ No updates needed - everything is current!"

            if [ "${{ env.workflows_has_major_updates }}" == "true" ]; then
              echo ""
              echo "⚠️  Major version updates available (not applied)"
              echo "   Run with allow_major_updates=true to apply them"
            fi
          fi

          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "⏰ Next run: 01:00 UTC (08:00 WIB)"
          echo "🚀 Manual trigger: Actions → Auto Update Dependencies"
          echo "════════════════════════════════════════════════════════"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
